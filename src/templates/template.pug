block variables

doctype html
html
    head
        meta(charset="utf-8")
        title #{title}
        meta(content="" name="description")
        meta(content="" name="keywords")
        meta(content="width=device-width, initial-scale=1" name="viewport")
        meta(content="ie=edge" http-equiv="x-ua-compatible")
        link(rel='stylesheet' href='./assets/styles/app.min.css' media="screen, projection, print")
        script(src='./assets/scripts/bundle.js' defer)
        script(src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js")
    body
        <!--div.preloader(id="page-preloader")
            div.loader
                span(id="load-perc") 100-->
        div.wrapper
            block content
            

   
